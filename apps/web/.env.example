# Discord Configuration
CHANNEL_ID=your_discord_channel_id_here
DISCORD_TOKEN=your_discord_token_here

# WebSocket Configuration
NEXT_PUBLIC_WS_URL=ws://localhost:8001/ws
NEXT_PUBLIC_DOWNLOAD_URL=http://localhost:8001

# How to get these values:
# 1. CHANNEL_ID: Right-click on your Discord channel and select "Copy ID" (Developer mode must be enabled)
# 2. DISCORD_TOKEN: 
#    - Bot Token: Create a bot at https://discord.com/developers/applications and copy the token
#    - User Token: Use browser dev tools to find your authorization header
#    Your token needs "Read Message History" permission in the target channel
# 3. WS_URL: WebSocket endpoint URL (ws://localhost:8001/ws for local, wss://your-domain.com/ws for production)
# 4. DOWNLOAD_URL: Base URL for download endpoint (http://localhost:8001 for local, https://your-domain.com for production) 